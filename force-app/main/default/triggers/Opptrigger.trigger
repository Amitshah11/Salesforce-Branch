trigger Opptrigger on Opportunity (after update,after insert) {
 /*   list<Task> tlist= new list<Task>();
    for(Opportunity op: trigger.new){
        
     
        if(op.amount==2000){
            
           op.adderror('amount should not be '+op.amount);

        }
        insert tlist;
    }
*/
}